{
  "name": "wakti-api-functions",
  "private": true,
  "version": "1.0.0",
  "description": "Isolated dependencies for Vercel Serverless Functions under /api. Vercel auto-installs these for functions in this directory, keeping heavy Node-only binaries (like ffmpeg-static) out of the client bundle and root install.",
  "dependencies": {
    "ffmpeg-static": "^5.3.0"
  }
}
